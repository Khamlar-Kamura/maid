import 'package:flutter/material.dart';


class MultiBookingContent extends StatelessWidget {
  const MultiBookingContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Coming soon...',
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
      ),
    );
  }
}